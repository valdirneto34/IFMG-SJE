#include <iostream>

using namespace std;

int main()
{
   /* 1.Crie um programa em C++ no qual você criará variáveis conforme o solicitado a seguir, 
   colocará algum dado de teste e o exibirá em seguida:
   */
   // a) O número da conta bancária;
   int Numero;
   Numero = 46734;
   cout << "Numero: " << Numero << endl;
   // b) A altura de uma pessoa em metros;
   double Altura;
   Altura = 1.8;
   cout << "Altura: " << Altura << endl;
   // c) A placa de um veículo;
   string Placa;
   Placa = "SIY1A65";
   cout << "Placa: " << Placa << endl;
   // d) O número de filhos de uma pessoa;
   int Filhos;
   Filhos = 15;
   cout << "Filhos: " << Filhos << endl;
   // e) A população de um país;
   string Populacao;
   Populacao = "210.7 Milhao";
   cout << "Populacao: " << Populacao << endl;
   // f) A cor de um objeto.
   string Cor;
   Cor = "Azul";
   cout << "Cor: " << Cor << endl;

   /* 2.Crie um programa em C++ no qual você vai criar as variáveis com tipos equivalentes aos dados apresentados a seguir,
    atribuir os dados às variáveis e exibir o conteúdo em seguida:
   */
   // a) “Processamento de Dados”
   string LetraA;
   LetraA = "Procesamento de Dados";
   cout << LetraA << endl;
   // b) 0.4
   float LetraB;
   LetraB = 0.4;
   cout << LetraB << endl;
   // c) “4.5E10”
   string LetraC;
   LetraC = "4.5E10";
   cout << LetraC << endl;
   // d) TRUE
   bool LetraD;
   LetraD = "TRUE";
   cout << LetraD << endl;
   // e) '?'
   char LetraE;
   LetraE = '?';
   cout << LetraE << endl;
   // f) “         “
   string LetraF;
   LetraF = "\"      \"";
   cout << LetraF << endl;
   // g) -5.3E-02
   string LetraG;
   LetraG = "-5.3E-02";
   cout << LetraG << endl;
   // h) “05”
   string LetraH;
   LetraH = "05";
   cout << LetraH << endl;
   // i) “FALSE”
   string LetraI;
   LetraI = "FALSE";
   cout << LetraI << endl;
   // j) 0
   int LetraJ;
   LetraJ = 0;
   cout << LetraJ << endl;

   return 0;
}
